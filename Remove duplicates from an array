#include <stdio.h>

int main() {
	//code
	int n;
	scanf("%d",&n);
	int arr[n];
	for(int i=0;i<n;i++)
	scanf("%d",&arr[i]);
        int k=1,i;
    for(i=1;i<n1;i++)
    {
        if(A[i]!=A[k-1])
        A[k++]=A[i];
    }
	for(int i=0;i<k;i++)
	printf("%d",arr[i]);
	return 0;
}

//Complexity - O(n)
